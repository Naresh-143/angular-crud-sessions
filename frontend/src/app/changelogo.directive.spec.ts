import { ChangelogoDirective } from './changelogo.directive';

describe('ChangelogoDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangelogoDirective();
    expect(directive).toBeTruthy();
  });
});
